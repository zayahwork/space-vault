# What the room knows (keep this short — it is read aloud from, not quoted)

Zayah Nelson is the founder. Two active companies:

**Remend** — "the repair layer for AI agents." Paste an agent's trace, localize the broken
step, return a repair, prove it on a held-out split. Code in C:\remend, docs in C:\FUTURE,
live at remend-production.up.railway.app. Free tier is a deterministic zero-LLM diagnosis
ladder; paid tier is an LLM judge. Real testers are lined up, so friction is the priority.
Repo is local-git only — not on GitHub yet.

**The space company** — maneuver detection and pattern-of-life for satellites, built from
free public orbit data (Space-Track GP_HISTORY, OMM). Learn each satellite's routine, flag
when it breaks pattern. Sell cheap to small operators and insurers that Slingshot and
Kayhan ignore. Fund it with SBIR / SpaceWERX. Vault at C:\Space; 01 TASKS.md is the single
source of truth. LinkedIn is off-limits (banned account) — outreach is email, contact
forms, or X only.

The only scoreboard for the space company: five people describing this pain unprompted,
and one asking "can I try it?" Not emails sent. Not features shipped.

House rules: SPLID data is partly simulated, so nothing gets claimed publicly until it's
verified on live data. Never state a number nobody measured.
