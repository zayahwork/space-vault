---
status: open
type: AFK
owner: VIDEO window (cameraman)
blocked-by: []
---
# 60-second style test cut

**Goal:** a rendered ~60s cold-open that proves the house style, so the founder can approve a look once instead of arguing every episode.

**Done when:** an mp4 exists in the renders folder (outside the vault), duration 55–65s, containing: the cut-storm hook per `07 Video/Reference - Style Teardown.md` (cuts every ~1s or faster), the episode goal stated ("today I find out if this only works on Starlink"), the **Day 1 card** burned in top corner, **at least two iPhone POV shots** (founder order — clips are local in `Videos/space folder/`, phone material is the spine), music bed with zero silent gaps, ffmpeg-verified loudness **−14 LUFS ± 1, peak ≤ −1 dBFS**, and an OPSEC timestamp list showing zero uncut hits (no names, no @, no auth files, no Gmail).

**Notes:** style over content — this is a test, not Episode 1. Don't spoil the OneWeb answer inside the 60s (goal rule: tension stays until the body of the real episode). PRD: `issues/PRD - zayah-in-orbit-vlog.md`.
